export interface BnThemeConfig {
  autoDarkMode: boolean;
  alwaysDark: boolean;
  useSystem: boolean;
  activeTheme: string;
  darkMode: boolean;
  darkFrom: string;
  darkTo: string;
  allowSave: boolean;
  darkTag: string;
  addThemeToBody: boolean;
  themeBackground:boolean;
}
