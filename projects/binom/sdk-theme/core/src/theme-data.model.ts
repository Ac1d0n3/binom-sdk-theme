export interface BnThemeData {
  autoDarkMode: boolean ;
  alwaysDark: boolean;
  useSystem: boolean;
  activeTheme: string;
  darkMode: boolean;
  darkFrom: string;
  darkTo: string;
  saveSettings: boolean;
  themeBackground:boolean;
  allowSave:boolean;
}
